const headingStyles = {
  lineHeight: "heading",
  letterSpacing: "tight",
  mt: 0,
};

const headings = {
  heading1: {
    ...headingStyles,
    fontSize: [5, null, null, 6],
  },
};

export default {
  text: {
    ...headings,
  },
};
